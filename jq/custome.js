new WOW().init();

$(window).stellar();

$('.owl-carousel').owlCarousel({
    margin:10,
    loop:true,
    autoWidth:true,
    items:3.5,
    autoplay:true,
    autoplayTimeout:2000,
    autoplayHoverPause:true

})